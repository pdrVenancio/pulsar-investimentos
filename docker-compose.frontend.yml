services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL}
      VITE_PULSAR_WS_BASE_URL: ${VITE_PULSAR_WS_BASE_URL}
    ports:
      - "3000:3000"
